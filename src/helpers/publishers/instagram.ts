//import { TwitterApi } from "twitter-api-v2";
//import path from "path";
//import { User, rrssUser } from "../../models";
import { ISNPost } from "../../models/sn-post";

export const instagramPosts = async (post: ISNPost) => {
  console.log(post);
  console.log("====================Instagram========================");
};
